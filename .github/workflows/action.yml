on:
  push:
     branches:
       - main
jobs:
  generate-build-number-deploy:
    runs-on: ubuntu-latest
    steps:
      - run: npx token-transformer ./tokens/tokens.json
        shell: bash
      - run: style-dictionary build
        shell: bash
