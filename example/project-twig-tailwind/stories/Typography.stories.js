import Typography from "../templates/shipwright/components/tokens/typography.html.twig";

export default {
    title: "Tokens/Typography",
    argTypes: {}
};

const Template = (args) => Typography({...args});

export const Type = Template.bind({});